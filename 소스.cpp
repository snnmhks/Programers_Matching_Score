#include <iostream>
#include <string>
#include <vector>

using namespace std;

int solution(string word, vector<string> pages) 
{
    int answer = 0;
    return answer;
}

int main()
{
    string a = "a";
    int b = static_cast<int>(a[0]);
    b -= 32;
    a[0] = static_cast<char>(b);
    cout << a;
    int j;
    cin >> j;
}